# AirportMatrix — AI Context

> This file is read by Claude, Cursor, Copilot, Trae, and other AI tools at session start.
> Persistent project memory spans multiple directories — read them all.

## Reading Order

1. **`.ilang/soul.md`** — project identity, stack, conventions
2. **`.ilang/memory.md`** — project logbook, known issues, current state
3. **`.trae/project-rules.md`** — ZeroCode v2.0 AI behavior rules (I-Lang protocol)
4. **`.trae/ilang-example.md`** — I-Lang protocol syntax reference
5. Proceed with the user's request

## Essential Commands

```bash
npm run dev      # Start dev server (http://localhost:3000)
npm run build    # Production build
npm run lint     # ESLint
npm run db:init  # Initialize/reset database
```
